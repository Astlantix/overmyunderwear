#include "autons.hpp"
#include "vex.h"
#include "functions.hpp"
using namespace vex;
using namespace std;

void rawp(void) {
    // intake alley triball, back up, take triball out of corner, score preload and triball, score triball in intake

    // intake triball side triball against bar, outtake toward goal, intake middle against bar, score other two with wings, outtake triball in intake and score

    // rush back to touch elevation bar
}

void lawp(void) {
    // take triball out of corner, score preload, back up into elevation bar

}

void elim(void) {
    // same as left side win point, but go back to matchloading zone for faster bowling
}

void skills(void) {
    // score two preloads
    
    // matchload 44 triballs
    msp(cata);
    wait(30,sec);
    msc(cata);
    Left(20);
    
    // push triballs on our side to the other side

    // go through alleyway, score with wings sweepind

    // hang
    

}

void test(void) {
    /*msp(fl);
    wait(1,sec);
    msc(fl);
    msp(fr);
    wait(1,sec);
    msc(fr);
    msp(mr);
    wait(1,sec);
    msc(mr);
    msp(ml);
    wait(1,sec);
    msc(ml);
    msp(br);
    wait(1,sec);
    msc(br);
    msp(bl);
    wait(1,sec);
    msc(bl);*/

    pid(500);

    //pid(24);

    
}